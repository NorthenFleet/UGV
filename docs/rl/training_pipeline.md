# 训练流水线

PPO（默认）：
- 并行采样 `N_env` → 收集轨迹 `T` 步
- GAE 优势计算（γ=0.99，λ=0.95）
- 多轮更新（剪切损失、熵、价值函数）
- 评估与早停：在验证环境衡量奖励与稳定性
- 保存 checkpoint：阶段与 best

课程学习：
- 阶段参数在 `curriculum.yaml` 配置，随训练推进提升难度

日志与复现：
- TensorBoard 与 CSV；固定随机种子；记录版本与配置